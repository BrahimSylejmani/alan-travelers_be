spring:
  application:
    name: car-booking
  datasource:
    password: 123
    url: jdbc:postgresql://localhost:5432/car-booking
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
